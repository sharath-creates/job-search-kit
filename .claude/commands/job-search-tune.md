---
description: Change how the job search behaves
---

Read `setup/06-tuning.md` and handle what the person asked for.

Remember that task prompts carry the person's details rather than reading them
at run time, so any change to behaviour means updating the scheduled tasks
themselves, and any change to the profile means checking which task prompts
carry the affected field.

After making a change, say in one line what changed, which tasks were updated,
and when the next affected run fires.
